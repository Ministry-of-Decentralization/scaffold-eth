type Brick @entity {
  id: ID!
  owner: Bytes!
  x: Int!
  y: Int!
  rgb: String!
  price: BigInt!
  lastUpdated: BigInt!
  wall: Wall!
}

type Wall @entity {
  id: ID!
  bricks: [Brick!] @derivedFrom(field: "wall")
  rowCount: Int!
}
